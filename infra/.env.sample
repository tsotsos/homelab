# -------------------------------
# K3s Cluster Bootstrap Settings
# -------------------------------

# SSH username used to connect to each node
USER="<your_ssh_user>"

# IP or DNS name used as the main TLS SAN for the control-plane node
TLS_SAN="<control_plane_ip_or_hostname>"

# K3s release channel: 'stable', 'latest', or a specific version (e.g., v1.29)
K3S_CHANNEL="stable"

# Additional arguments to pass to k3s (e.g., to disable default components)
K3S_EXTRA_ARGS="--disable traefik --disable servicelb"

# Whether to deploy kube-vip after bootstrapping (true/false)
INSTALL_KUBE_VIP=false

# -------------------------------
# File Paths
# -------------------------------

# Path to your homelab git repository
HOMELAB="<absolute_path_to_your_homelab_repo>"

# Where to save the local kubeconfig file
KUBECONFIG_PATH="<absolute_path_to_store_kubeconfig>"

# -------------------------------
# Cluster Nodes
# -------------------------------

# IP addresses or hostnames of your server (control-plane) nodes
SERVERS=(<server_ip_1> <server_ip_2> <server_ip_3>)

# IP addresses or hostnames of your agent (worker) nodes
AGENTS=(<agent_ip_1> <agent_ip_2>)

# -------------------------------
# Usage
# -------------------------------
# 1. Copy this file to a real .env file (e.g. `cp .env-sample secrets.env`)
# 2. Fill in all required values.
# 3. Run the setup script: `./setup-k3s.sh secrets.env`
