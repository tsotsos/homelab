# =============================================================================
# TERRAFORM VARIABLES - MINIMAL CONFIGURATION
# =============================================================================
# 
# This file contains only essential variables that cannot be moved to YAML.
# All cluster, network, VM, and version configuration is now managed in:
# cluster-config.yaml (single source of truth)
#
# =============================================================================

# =============================================================================
# PROXMOX CONNECTION CONFIGURATION
# =============================================================================
# These credentials are required for Terraform Proxmox provider authentication
# and cannot be moved to cluster-config.yaml for security reasons

proxmox_ve_endpoint     = "https://xxxxxx:8006/api2/json"
proxmox_ve_api_token    = ""
proxmox_ve_insecure     = true
proxmox_ve_ssh_username = "terraform"