apiVersion: cdi.kubevirt.io/v1beta1
kind: DataVolume
metadata:
  name: microos-rootdisk
  namespace: default
spec:
  source:
    http:
      url: https://download.opensuse.org/tumbleweed/appliances/openSUSE-MicroOS.x86_64-ContainerHost-OpenStack-Cloud.qcow2
  pvc:
    accessModes: ["ReadWriteOnce"]
    resources:
      requests:
        storage: 64Gi
