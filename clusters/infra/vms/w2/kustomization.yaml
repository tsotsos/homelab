resources:
  - ../base-wrk
patches:
  - target:
      group: kubevirt.io
      version: v1
      kind: VirtualMachine
      name: base-vm
    patch: |-
      - op: replace
        path: /metadata/name
        value: w2
  - target:
      group: kubevirt.io
      version: v1
      kind: VirtualMachine
      name: w2
    path: patch.yaml
