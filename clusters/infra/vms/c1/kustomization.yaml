resources:
  - ../base-cp
patches:
  - target:
      group: kubevirt.io
      version: v1
      kind: VirtualMachine
      name: base-vm
    patch: |-
      - op: replace
        path: /metadata/name
        value: c1
  - target:
      group: kubevirt.io
      version: v1
      kind: VirtualMachine
      name: c1
    path: patch.yaml