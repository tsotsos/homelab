dependsOn:
  - selector:
      matchLabels:
        vms.kng.house/bundle: common-platform

defaultNamespace: kng-vms
targetCustomizations:
  - name: local-only
    clusterSelector:
      matchLabels:
        management.cattle.io/cluster-name: local
    kustomize:
      dir: .
