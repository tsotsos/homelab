apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../apps/kube-vip

patches:
- path: patch.yaml
  target:
    group: apps
    version: v1
    kind: DaemonSet
    name: kube-vip
    namespace: kube-system
