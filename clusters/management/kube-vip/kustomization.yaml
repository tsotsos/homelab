apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../apps/kube-vip

configMapGenerator:
  - name: kube-vip-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true