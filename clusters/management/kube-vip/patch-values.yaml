apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-vip
  namespace: kube-system
spec:
  valuesFrom:
    - secretRef:
        name: kube-vip-values
        key: values.yaml