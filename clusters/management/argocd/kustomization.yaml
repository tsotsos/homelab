apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../apps/argocd

patches:
  - path: configmap.yaml
    target:
      kind: ConfigMap
      name: argocd-cm
  # - path: rbac.yaml
  #   target:
  #     kind: ConfigMap
  #     name: argocd-rbac-cm
  - path: ingress.yaml
    target:
      kind: Ingress
      name: argocd-server
