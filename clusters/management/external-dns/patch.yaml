apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-dns-unifi
  namespace: external-dns
spec:
  template:
    spec:
      containers:
        - name: webhook
          ports:
            - containerPort: 8888
              name: http-webhook
              protocol: TCP
