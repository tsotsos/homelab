apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: external-dns
spec:
  values:
    txtOwnerId: "homelab"
    txtPrefix: "k8s."
    domainFilters:
      - "kng.house"