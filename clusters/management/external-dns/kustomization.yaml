apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: external-dns

resources:
  - ../../../apps/external-dns
  - sealed-secret.yaml

patchesJson6902:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: external-dns-unifi
      namespace: external-dns
    path: patch.yaml