apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: external-dns

resources:
  - ../../../apps/external-dns
  - sealed-secret.yaml

patches:
  - path: patch-values.yaml
    target:
      kind: Deployment
      name: external-dns