---
# Ingress for PeaNUT with Authentik authentication
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: peanut
  namespace: authentik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dns
    external-dns.alpha.kubernetes.io/hostname: peanut.kng.house
spec:
  ingressClassName: cilium
  tls:
    - hosts:
        - peanut.kng.house
      secretName: peanut-tls
  rules:
    - host: peanut.kng.house
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ak-outpost-authentik-embedded-outpost
                port:
                  number: 9000
