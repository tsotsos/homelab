apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: peanut-config
  namespace: observability
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
