apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crafty-data
  namespace: crafty-controller
  labels:
    app: crafty-controller
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
