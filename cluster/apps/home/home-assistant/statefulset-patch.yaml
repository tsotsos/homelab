apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: home-assistant
spec:
  template:
    spec:
    - name: codeserver
      securityContext:
        capabilities:
          add:
            - CHOWN
      volumes:
        - name: config-volume
          configMap:
            defaultMode: 0755
            