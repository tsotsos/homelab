apiVersion: v1
kind: ConfigMap
metadata:
  name: hass-configuration
  namespace: home
data:
  configuration.yaml: |
    # existing configuration content... (same as the base)
    # Appended configuration
