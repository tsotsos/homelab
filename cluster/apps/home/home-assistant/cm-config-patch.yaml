apiVersion: v1
kind: ConfigMap
metadata:
  name: hass-configuration
  namespace: home
data:
  configuration.yaml: |
    # Appended configuration
    recorder:
      purge_keep_days: 90
      commit_interval: 3
      db_url: !env_var DB_CONNECTION