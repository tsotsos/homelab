config:
  address: # Overridden per cluster
env:
  vip_interface: # Overridden per cluster
  vip_arp: "true"
  lb_enable: "true"
  lb_port: "6443"
  vip_cidr: "32"
  cp_enable: "true"
  vip_ddns: "false"
  svc_enable: "true"
  svc_election: "true"
  vip_leaderelection: "true"